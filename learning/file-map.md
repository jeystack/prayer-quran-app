# File Map

Every file and folder in this project — what it is, why it exists, and whether I understand it.
Nothing in this repo should ever be a mystery box.

**Statuses:**
- `known` — I explained it in my own words. I know what it does and why it's here.
- `parked` — Honest one-liner for now. Deep dive scheduled for a future section.
- `generated` — Machine-made (by AI or a tool). I didn't write it by hand, but I should understand its contents.

---

## Root: `/`

| Path | What it is | Status | Notes |
|---|---|---|---|
| `learning/` | Folder for everything about learning this project — plans, knowledge, maps. | known | I know every file here and what it's for. |
| `app/` | The actual app code (HTML, CSS, JS, data). | known | This is where all the real code lives. I've built the full HTML structure here. |
| `new-project.code-workspace` | VS Code workspace file. | parked | Auto-generated by VS Code when I opened the folder. Not part of the app. |
| `prayer-quran-app.code-workspace` | VS Code workspace file for this project. | parked | Auto-generated by VS Code. Not part of the app. |

## `learning/` — Learning Files

| Path | What it is | Status | Notes |
|---|---|---|---|
| `learning/project.md` | The project spec — who I am, what we're building, MVP features, parking lot. | known | I helped write this. I know every section. |
| `learning/plan.md` | The build plan — locked design decisions, 9 sections, learning goals. | known | I walked through every decision and chose the tools. |
| `learning/knowledge-graph.md` | Living map of what I know — every concept, its status, evidence. | generated | AI created the initial seed, but I update it with my own progress. |
| `learning/file-map.md` | This file — map of every file in the project, what it does. | generated | AI created the initial version. I should update it as files change. |

## `app/` — The Application

| Path | What it is | Status | Notes |
|---|---|---|---|
| `app/index.html` | The main HTML page — the entry point of the app. | known | I wrote this. Header, main, three sections. Prayer times section has all 5 prayers with class labels. Daily checklist has 5 checkboxes with labels. Qur'an reader has an empty `<ul>` — Surah list is built dynamically from JSON in Task 6.3. Section 5: changed script tag to type="module" to support ES module imports. |
| `app/css/` | Folder for CSS files. | known | Created in Task 1.1. |
| `app/css/styles.css` | The main stylesheet — controls how the app looks. | known | I wrote this. Rules for body, header, all three card types (prayer, checklist, Qur'an), hover states with lift effect, focus-visible on checkboxes, transitions for smooth animation, `@keyframes pulse-green` animation for next prayer highlight, media query for small screens, and Task 6.4: `.hidden` class, `.verse` spacing, `.verse-arabic` (RTL, larger font), `.verse-english` (lighter color). Task 6.5: added Amiri Arabic font in `@import`, `.verse-number` styling (green, small), `#back-btn` styling (outlined green button with hover effect), and `.verse` border-bottom dividers between verses. |
| `app/js/` | Folder for JavaScript files. | known | Created in Task 1.1. |
| `app/js/main.js` | The main JavaScript file — where all app logic lives. | known | Created in Task 4.1. Task 4.2: Fajr checkbox listener — querySelector, change event, if/else text swap. Task 4.3: count checked boxes with querySelectorAll and forEach, live update display. Task 4.4: prayer time click handlers — forEach with click listeners, event.target.parentElement to find sibling prayer name, if/else if chain to map each prayer to its own time. Task 4.5: Surah list click handlers — querySelectorAll(".surah-item"), forEach, event.target.textContent, string concatenation to build "Reading: [name]". Simplified five if/else branches into one line. Task 5.2: imported Adhan.js, created Coordinates, Date, CalculationMethod params, calculated PrayerTimes, logged all 6 times to console. Task 5.3: removed old hardcoded click-to-reveal logic from Task 4.4, replaced with page-load forEach that matches each prayer-time span to its name via parentElement, formats times with `.toLocaleTimeString()`, and sets textContent. Task 5.4: used `prayerTimes.nextPrayer()` to find the next upcoming prayer, converted it to a lowercase string with `.toString().toLowerCase()`, looped through prayer items to find the match, and added `.next-prayer` CSS class with `classList.add()`. Task 6.2: fetch() loads Qur'an metadata from local JSON. Task 6.3: dynamic Surah list — forEach loop builds 114 `<li>` elements from JSON data using `document.createElement()` and `appendChild()`. Task 6.4: Surah detail view — stores `data-index` on each `<li>`, click handler fetches Arabic + English JSON with `Promise.all()`, builds verse HTML with a `for` loop and `.innerHTML`, toggles visibility with `classList.add/remove("hidden")`. Back button reverses the toggle. Task 6.5: added `<span class="verse-number">` inside the verse-building `for` loop to display the verse number before each Arabic verse. Task 7.2: added `localStorage.setItem(box.id, box.checked)` inside the forEach checkbox loop to save each checkbox's state when it changes. Task 7.3: added a second forEach loop that runs on page load (before `updatePrayerCount()`) to restore saved checkbox states from localStorage — `getItem(box.id)`, compare to `"true"`, set `box.checked = true`. **Task 7.4: added daily auto-reset block that compares saved date to today's date, clears old checkbox keys on mismatch, and saves the new date. Also saves `checklist-date` in the checkbox change listener so checking a box stamps the current date.** |
| `app/data/surah.json` | Metadata for all 114 Surahs — English name, Arabic name, verse count, place of revelation. | known | Downloaded from semarketir/quranjson. Contains all 114 entries. Fetched in Task 6.2 to build the dynamic Surah list. The `index` property (1-indexed string like "001") is used to construct file paths for individual Surah data. |
| `app/data/surah/` | Folder with 114 JSON files (surah_1.json – surah_114.json), each containing the Arabic text of one Surah's verses each as a key-value object (`verse_1`, `verse_2`, ...). | known | Downloaded from semarketir/quranjson. Fetched on demand in Task 6.4 when user taps a Surah via `Promise.all()` — the `verse_` prefix maps to an object key, not an array index. |
| `app/data/translation/en/` | Folder with 114 JSON files (en_translation_1.json – en_translation_114.json), each containing the English translation of one Surah's verses — same structure as Arabic files. | known | Downloaded from semarketir/quranjson. Fetched alongside the Arabic text in Task 6.4. The structure mirrors the Arabic files exactly: `{ "verse": { "verse_1": "...", ... } }`. |

## Root: Screenshots

| Path | What it is | Status | Notes |
|---|---|---|---|
| `Prayer-Quran-App first look.png` | Screenshot of the app at the end of Section 3. | known | Shows the styled app — green header, prayer cards, checklist, Qur'an reader. |

---

*Last updated: 29 July 2026 — Section 7 complete (Daily Checklist with localStorage)*
